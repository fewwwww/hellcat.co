import * as THREE from 'three'
const state = {
  activeMesh: {},
  activeMeshName: 'object001_Material011_0',
  cameraPos: new THREE.Vector3(6,5,12),
  target: new THREE.Vector3(4,0,0),
  shouldUpdate: true
}

export default state
